{
  "metadata": {
    "description": "User with bcrypt password hash that should be re-hashed to argon2id on login (progressive re-hashing test)",
    "expected_response_status": 200,
    "expected_old_hash_prefix": "$2a$",
    "expected_new_hash_prefix": "$argon2id$",
    "expected_token_fields": ["token", "refreshToken", "user"],
    "test_tags": ["auth", "login", "password-hashing", "progressive-rehashing"]
  },
  "email": "bcrypt-user@example.com",
  "password": "SecurePass123!"
}
